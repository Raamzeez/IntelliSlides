import React, { FC } from "react";

const Line: FC = () => {
  return (
    <div
      style={{
        height: 2,
        width: "100%",
        backgroundColor: "red",
        opacity: 0.5,
      }}
    />
  );
};

export default Line;
