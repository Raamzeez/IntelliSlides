interface iPresentation {
    title: string
    subtitle: string
    id: string
    thumbnail: {
        contentUrl: string
        height: number
        width: number
    }
}

export default iPresentation
